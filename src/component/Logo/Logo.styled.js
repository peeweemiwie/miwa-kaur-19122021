import styled from 'styled-components';

const dimension = '6rem';
export const StyledLogo = styled.figure`
	height: ${dimension};
	margin: 0;
	padding: 0;
	width: ${dimension};

	img {
		max-width: 100%;
	}
`;
